[ziina]
# Get this from Ziina -> Custom Integration -> Access Token
access_token = "PUT_YOUR_ZIINA_ACCESS_TOKEN_HERE"

# Public URL of your Streamlit app (change after deploy)
app_base_url = "https://snow-liwa.streamlit.app"

# true = test mode, false = live mode (when you go production)
test_mode = true
